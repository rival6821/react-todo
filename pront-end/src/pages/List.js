import React from 'react';
import ListContainer from '../containers/ListContainer';

const List = () => {
    return (
        <div>
            <ListContainer/>
        </div>
    );
};

export default List;