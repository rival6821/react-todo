import React from 'react';
import LoginComponent from '../containers/LoginContainer';

const Login = () => {
    return (
        <div>
            <LoginComponent/>
        </div>
    );
};

export default Login;