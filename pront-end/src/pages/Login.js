import React from 'react';
import LoginContainer from '../containers/LoginContainer';

const Login = () => {
    return (
        <div>
            <LoginContainer/>
        </div>
    );
};

export default Login;